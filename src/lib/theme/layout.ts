import { spacing } from "./spacing";

// Screen Layout and Page Grid Alignment Tokens
export const layout = {
  screenPadding: "px-4 md:px-8 py-12",       // El aire del viewport
  sectionSpacing: "gap-8 md:gap-12",         // Aire entre PageHeader y contenido
  gridGap: "gap-6 md:gap-8",                 // Separación de grids 2D
  cardPadding: "p-6 md:p-8",                 // El colchón interno de FloraGlass
  inputHeight: "h-10",                       // El alto táctico de los inputs
  buttonHeight: "h-10"                       // El alto unificado de botones
};
